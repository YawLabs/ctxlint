{
  "mcpServers": {
    "stdio-no-command": {
      "type": "stdio",
      "args": ["-y", "some-package"]
    },
    "http-no-url": {
      "type": "http",
      "headers": {
        "Authorization": "Bearer ${API_KEY}"
      }
    }
  }
}
