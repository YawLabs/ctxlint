# Build output and vendored code. Already covered by .gitignore (which Prettier
# honours by default), listed here so the ignore survives a .gitignore change.
node_modules/
dist/
coverage/

# Generated by pnpm, never hand-formatted.
pnpm-lock.yaml

# Test fixtures are deliberately-authored linter INPUTS, and several are
# intentionally malformed. Verified: `prettier --write` strips the trailing
# comma from fixtures/mcp-configs/invalid-json/.mcp.json, after which
# JSON.parse succeeds -- silently defeating the mcp-schema/invalid-json rule
# that fixture exists to exercise (src/core/__tests__/mcp-parser.test.ts).
# eslint.config.js ignores fixtures/ for the same reason.
fixtures/
